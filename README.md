## yaroslav.bogdanov.2005@mail.ru \ Богданов Ярослав Антонович \ @yarayk

# 🎮 Правила игры «Слова»
Цель игры
Называть слова по цепочке. Последний оставшийся игрок побеждает.

# Начало игры
Любой участник пишет /start_game — создаётся новая игра
Каждый желающий пишет /join — присоединяется к игре
Когда все готовы — любой пишет /begin (нужно минимум 2 игрока)
Бот случайно выбирает стартовое слово и случайный порядок ходов
Ход игры
Бот объявляет чей ход и букву, на которую нужно назвать слово
Текущий игрок пишет слово в чат обычным сообщением (не командой)
Слово должно начинаться на указанную букву
Правило букв: если слово заканчивается на Ь, Ъ или Й — берётся предпоследняя подходящая буква. Например: «апельсин» → следующее слово на «н», «трамвай» → на «а»

Слово не принимается если:

Начинается не на ту букву
Содержит не только русские буквы
Уже было использовано в этой игре
Голосование
После каждого слова все остальные игроки голосуют, существует ли такое слово:

+ — слово существует
- — слово не существует
Голосование завершается когда все проголосовали или истекло 30 секунд.

Результат: слово принято если больше 50% проголосовали «+». При равенстве голосов или если никто не голосовал — слово принимается автоматически.

✅ Слово принято → игрок получает очки, ход переходит следующему
❌ Слово отклонено → игрок выбывает из игры
Таймаут хода
На каждый ход даётся 60 секунд. Если игрок не успел написать слово — он выбывает автоматически.

# Очки
За каждое принятое слово начисляется количество букв в слове:

«кот» → +3 очка
«апельсин» → +8 очков
Конец игры
Игра заканчивается когда остаётся один игрок — он объявляется победителем. Бот показывает итоговую таблицу результатов.

Также можно завершить игру досрочно командой /stop_game — победитель не объявляется, показываются текущие очки.

# Команды
Команда	Описание
/start_game	Создать новую игру
/join	Присоединиться к игре
/begin	Начать игру (мин. 2 игрока)
/stop_game	Завершить игру досрочно
/game_info	Статус текущей игры
+ / -	Проголосовать за слово

# Words Bot — Отчёт по проекту

## Что это такое

Telegram-бот для групповой игры в слова. Игроки по очереди называют слова на последнюю букву предыдущего, остальные голосуют за каждое слово — настоящее оно или нет. Кто не успел или чьё слово отклонили — выбывает. Побеждает последний оставшийся.

---

## Стек

- **Python 3.11**, aiohttp (чистый, без aiogram)
- **PostgreSQL** + SQLAlchemy async + Alembic
- **Telegram Bot API** — polling вручную

---

## Архитектура

```
app/
├── tg/
│   ├── client.py       # HTTP-запросы к Telegram API
│   ├── poller.py       # Бесконечный цикл getUpdates
│   ├── handlers.py     # Роутинг и обработка команд
│   └── dataclasses.py  # Update, Message, Chat, MessageFrom
├── game/
│   ├── accessor.py     # Все запросы к БД
│   └── service.py      # Вся игровая логика
├── store/
│   ├── database/
│   │   ├── models.py   # SQLAlchemy модели
│   │   └── database.py # Подключение к БД
│   ├── migrations/     # Alembic миграции
│   └── store.py        # Точка сборки всех компонентов
└── web/
    └── app.py          # aiohttp приложение, startup/cleanup
```

---

## Классы

**`TgClient`** — отправляет запросы к Telegram. Методы: `get_updates()`, `send_message()`.

**`Poller`** — запускает asyncio task с бесконечным циклом. Получает updates, сдвигает offset, передаёт в handlers. Ошибки не роняют процесс — sleep(1) и продолжает.

**`GameAccessor`** — слой БД. Только SQL-запросы, никакой логики. Методы: `get_active_game`, `create_game`, `update_game`, `get_player`, `get_active_players`, `create_player`, `update_player`, `get_used_words`, `add_used_word`, `get_votes`, `add_vote`, `get_scoreboard`.

**`GameService`** — мозг игры. Только бизнес-логика, никаких SQL-запросов напрямую. Методы: `start_game`, `join_game`, `begin_game`, `submit_word`, `cast_vote`, `resolve_vote`, `eliminate_on_timeout`, `stop_game`.

**`Store`** — собирает все компоненты: `tg_client`, `poller`, `database`, `game` (accessor), `game_service`.

---

## База данных (4 таблицы)

**`games`** — одна строка на игру. Хранит статус, текущее слово, чей ход, слово на голосовании, дедлайны.

**`players`** — игроки в игре. Хранит очки, активен ли, порядок хода.

**`used_words`** — все слова которые уже были в этой игре. Нужно чтобы не повторяться.

**`votes`** — голоса за каждое слово. Один игрок — один голос за одно слово.

---

## State Machine

```
IDLE
  ↓ /start_game
WAITING
  ↓ /begin (≥2 игроков)
IN_GAME ←──────────────────────────┐
  ↓ игрок пишет слово              │
VOTING                             │
  ↓ все проголосовали              │
  ├── принято → очки игроку ───────┘
  └── отклонено → игрок выбывает ──┘
  
  Если остался 1 игрок → FINISHED
```

---

## Команды бота

| Команда | Что делает |
|---|---|
| `/start_game` | Создаёт новую игру в чате, переводит в WAITING |
| `/join` | Игрок вступает в ожидающую игру |
| `/begin` | Запускает игру (нужно минимум 2 игрока) |
| `/stop_game` | Досрочно завершает игру, показывает счёт |
| `/game_info` | Текущий статус игры |
| `+` / `-` | Голосование за слово |

---

## Последовательность игры

1. Кто-то пишет `/start_game` — бот сообщает что игра открыта
2. Игроки пишут `/join` — бот подтверждает каждого
3. Кто-то пишет `/begin` — бот перемешивает порядок, выбирает случайное стартовое слово, объявляет чей ход
4. Игрок чей ход пишет слово на нужную букву
5. Бот проверяет: правильная буква? не использовалось раньше? только русские буквы?
6. Если ok — переходит в VOTING, просит остальных проголосовать
7. Остальные пишут `+` или `-`
8. Когда все проголосовали — бот подводит итог:
   - Больше 50% за → слово принято, очки = длина слова, ход переходит следующему
   - Меньше 50% → слово отклонено, игрок выбывает
9. Если остался 1 игрок — игра завершается, бот показывает победителя и таблицу очков

---

## Игровые правила

- Одна активная игра на чат
- Слово должно начинаться на последнюю букву предыдущего
- Буквы **ь**, **ъ**, **й** в конце слова пропускаются (берётся предпоследняя)
- Слово нельзя повторить в рамках одной игры
- Очки = количество букв в слове
- Автор слова не голосует за своё слово

---

## Что сделано (MVP)

- ✅ Polling сервер
- ✅ Все команды (/start_game, /join, /begin, /stop_game, /game_info)
- ✅ Проверка слов
- ✅ Голосование через + / -
- ✅ Подсчёт очков
- ✅ Выбывание игроков
- ✅ Завершение игры с таблицей результатов
- ✅ БД с полной историей игр

## Что не сделано (следующие шаги)

- ⬜ Таймеры ходов (автовыбывание по времени)
- ⬜ Восстановление таймеров после рестарта
- ⬜ Admin API
- ⬜ Inline-кнопки для голосования вместо + / -
